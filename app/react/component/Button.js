import React, {Component, PropTypes} from 'react';

class Button extends Component {
    render() {
        return (
            <div>Button
            </div>
        );
    }
}

Button.propTypes = {

};

export default Button;